{
  "id": "wordsmith",
  "name": "Wordsmith",
  "description": "Draft prose (emails, newsletters, marketing copy, rewrites) in a focused writing call on the provider this install already uses. You orchestrate, the writing model writes.",
  "enabled": true,
  "triggers": [
    "draft",
    "write",
    "compose",
    "rewrite",
    "polish",
    "tighten",
    "humanize",
    "newsletter",
    "reply to",
    "email draft",
    "landing page copy",
    "announcement",
    "make this sound",
    "shorter version",
    "longer version",
    "rephrase"
  ],
  "priority": 70,
  "context": "Script: {{PROJECT_PATH}}/skills/wordsmith/wordsmith.mjs\nNo key of its own: it reuses whatever provider key is already in .env (ANTHROPIC_API_KEY, OPENAI_API_KEY, GOOGLE_API_KEY, or AZURE_API_KEY), preferring AI_PROVIDER. A subscription-only install has no API key at all, so the script exits saying so - write the copy yourself in that case, following the voice block in SKILL.md.\nSee SKILL.md for when to invoke, tiers, the voice block, and the post-processing protocol."
}
